const alphaErr = "must only contain letters.";
const lengthErr = "must be between 1 and 10 characters.";

const urlErr = "must be a valid URL.";

const httpsErr = "URL must start with https://";

const emptyErr = "must not be empty.";
 

module.exports = {
    alphaErr,
    lengthErr,
    urlErr,
    httpsErr,
    emptyErr
}